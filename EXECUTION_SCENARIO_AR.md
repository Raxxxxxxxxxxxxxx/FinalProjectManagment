# السيناريو التنفيذي لمنصة ربط أصحاب المشاريع بالمورّدين

## 1. الهدف

منصة توريد إلكترونية عربية ثنائية اللغة تتيح لصاحب المشروع نشر طلب عرض سعر
(RFQ)، واستقبال عروض من مورّدين موثّقين، ومقارنتها وفق سعر ومدة وتقييم، ثم
إنشاء أمر شراء ومتابعة التنفيذ والدفع اليدوي والتقييم ضمن أثر زمني واضح.

## 2. الأدوار

### صاحب المشروع

1. ينشئ حساباً ويدير ملف منشأته ووثيقة التسجيل الخاصة.
2. ينشئ RFQ ببند واحد أو أكثر ومدينة وموعد وميزانية.
3. يستقبل عروض الموردين ويقارن السعر والمدة والتقييم والدرجة الاستشارية.
4. يختار عرضاً بنفسه، فينشأ أمر شراء مع snapshot للاتفاق.
5. يسجل بيانات التحويل أو الدفع عند الاستلام.
6. يتابع الحالات والإشعارات ويؤكد الاستلام.
7. يقيّم المورد مرة واحدة ويعرض تقاريره أو يصدر CSV.

### المورّد

1. يكمل ملف المنشأة ويرفع وثيقة PDF/JPG/PNG خاصة.
2. ينتظر مراجعة الإدارة؛ تغيير الاسم القانوني أو رقم/وثيقة التسجيل يلغي
   التوثيق حتى المراجعة من جديد.
3. يدير كتالوج منتجاته وخدماته ووحداتها وأسعارها المرجعية.
4. بعد التوثيق، يقدم عرضاً واحداً لكل RFQ مفتوح.
5. يعدل أو يسحب العرض Pending قبل الموعد النهائي.
6. عند الاختيار، يؤكد الأمر ثم يسجل التجهيز والشحن والتسليم.
7. يتابع التقييم والتقارير ويصدر أوامره إلى CSV.

### إدارة المنصة

1. تدير المستخدمين وملفات المنشآت والتوثيق والتصنيفات من Django Admin.
2. تراجع بيانات التحويلات اليدوية وتؤكدها أو ترفضها بسبب.
3. تطلع على الطلبات والأوامر والتقارير وتتدخل في انتقالات الأمر عند الحاجة.
4. لا تنفذ النسخة الحالية نزاعات أو عمولات أو استرداداً مالياً حقيقياً.

## 3. دورات الحالة الفعلية

### RFQ

`Open → Awarded → Closed`

الحالات `Draft` و`Evaluating` و`Cancelled` معرفة في النموذج، لكن لا توجد
واجهة كاملة لإدارتها. تقبل خدمة الاعتماد Open أو Evaluating.

### العرض

`Pending → Selected | Rejected | Withdrawn`

### أمر الشراء

`Awaiting confirmation → Confirmed → Preparing → Shipped → Delivered → Completed`

لا توجد حالياً واجهة إلغاء أمر أو الرجوع إلى حالة سابقة.

### الدفع

- التحويل: `Submitted → Confirmed | Rejected`، ويمكن إعادة إرسال المرفوض.
- الدفع عند الاستلام: `Submitted` يسمح بالتجهيز ويصبح `Confirmed` تلقائياً
  عند إكمال الأمر.
- يمنع قرار إداري متأخر على COD بعد بدء التجهيز.

## 4. قواعد العمل الأساسية

- المورد الموثق فقط يستطيع إرسال عرض.
- لكل مورد عرض واحد لكل RFQ؛ يمكن تعديله أو سحبه قبل الموعد.
- العروض المسحوبة تستبعد من المقارنة.
- الدرجة الاستشارية من 100:
  - 50% للسعر: `أقل سعر / سعر العرض`.
  - 30% للمدة: `أسرع مدة / مدة العرض`.
- 20% للتقييم: `التقييم / 5`، أو 3/5 عند غياب التقييم.
- الدرجة توصية مفسرة ولا تعتمد الفائز تلقائياً.
- يعاد فحص أن المورد نشط وموثق لحظة الاعتماد؛ لا يكفي أن العرض قُدّم عندما
  كان الحساب صالحاً.
- اختيار المالك فقط ينشئ الفائز؛ UniqueConstraint يمنع فائزاً ثانياً.
- السعر والمدة وبنود RFQ تنسخ إلى أمر الشراء.
- لا يبدأ التجهيز قبل دفع مؤكد، باستثناء COD المرسل.
- لا يقيّم المورد إلا مالك أمر مكتمل، ولمرة واحدة.
- وثيقة المنشأة لا تعرض للعامة ولا ينزلها إلا صاحبها أو الإدارة.
- قيم الوحدات تخزن كرموز ثابتة وتترجم إلى العربية والإنجليزية.
- إشعارات الحالة تحفظ payload تاريخياً حتى لا تتغير رسالتها مع آخر حالة.

## 5. الوظائف المنفذة

- التسجيل والمصادقة والأدوار والبريد الفريد دون حساسية الأحرف.
- ملف المنشأة، نسبة الاكتمال، وثيقة خاصة حتى 5MB وفحص توقيع أساسي.
- مراجعة التوثيق، وحجب ملف/منتجات المورد غير الموثق عن العموم.
- كتالوج عام وبحث وتصنيف وpagination، وإدارة المورد لعناصره.
- إنشاء RFQ ببنود ووحدات، وبحث وتصنيف وpagination.
- تقديم وتعديل وسحب العرض عبر service معاملات.
- مقارنة متعددة المعايير بأوزان 50/30/20.
- اعتماد ذري، أمر شراء snapshot، حالات وإشعارات.
- تحويل يدوي أو COD ومراجعة إدارة.
- تقييمات وتقارير حسب الدور وتصدير CSV آمن.
- إحصاءات رئيسية محسوبة، منها وفر تقديري.
- العربية والإنجليزية وRTL/LTR وثيم فاتح/داكن.
- Tailwind محلي بلا CDN، طباعة أمر شراء، وتحسينات وصول.
- CSP وReferrer/Permissions headers وصفحات 403/404/500.
- `/health/` لفحص قاعدة البيانات.
- `seed_demo --reset` يعيد كتالوجاً وأربع حالات عرض ثابتة.
- يرفض `seed_demo` العمل خارج `DEBUG` لحماية البيئات المنشورة من حسابات
  العرض ذات كلمة المرور الثابتة.

## 6. بيانات العرض الثابتة

يشغل:

```bash
python manage.py migrate
python manage.py seed_demo --reset
python manage.py runserver
```

الحسابات، وكلمة المرور للجميع `Demo12345!`:

- `owner_demo`
- `supplier_demo`
- `supplier2_demo`
- `admin_demo`

الحالات:

1. «تجهيز مكاتب الفرع الجديد»: Open وعرضان للمقارنة.
2. «معدات السلامة المهنية للمستودع»: أمر Shipped مع COD.
3. «تطوير موقع تعريفي للشركة»: Open وعرض واحد.
4. «توريد أجهزة حاسوب لفريق المبيعات»: Completed ودفع مؤكد وتقييم 5/5.

## 7. حدود النسخة الحالية

- الدفع إداري، لا اتصال ببنك ولا بيانات بطاقات.
- لا تحقق بريد/SMS ولا password reset.
- لا محادثة أو نزاع أو إلغاء أو استرداد.
- لا تسعير لكل بند أو ضريبة أو شحن كحقول مستقلة.
- لا API أو تطبيق هاتف.
- التقارير بلا نطاق تاريخي أو PDF، وتفترض العملة الحالية.
- SQLite للعرض المحلي؛ PostgreSQL مطلوب لتزامن إنتاجي.
- لا rate limiting أو 2FA أو malware scanning أو مراقبة مركزية.
- CSP الأساسية ما زالت تسمح بـ`unsafe-inline`.
- الشعار الموجود مؤقت حتى استلام الشعار الرسمي.

## 8. المرحلة المستقبلية

- PostgreSQL وتخزين خاص للوثائق وbackup/monitoring.
- بوابة دفع حقيقية بـwebhooks وidempotency.
- بريد وSMS و2FA وrate limiting.
- AuditLog عام ونظام نزاعات وإلغاء واسترداد.
- تسعير تفصيلي وتقرير PDF ونطاقات تاريخية.
- أوزان مقارنة قابلة للتهيئة حسب نوع المشتريات.
- REST API وتطبيق هاتف ومطابقة موردين قابلة للتفسير.

## 9. معايير القبول لنسخة المناقشة

- تنجح migrations و**52/52 اختباراً** و`check --deploy`.
- يعيد `seed_demo --reset` الحالات الأربع من أي تدريب سابق.
- يعمل Tailwind والواجهة دون اتصال خارجي.
- يستطيع المالك تنفيذ دورة الاختيار حتى التقييم.
- يمنع الخادم العمليات حسب الدور والملكية والحالة.
- لا يكشف CSV أو أمر أو وثيقة لطرف غير مخول.
- تظهر الدرجة 50/30/20 كأداة استشارية لا اختيار آلي.
- تعمل العربية والإنجليزية والثيمان والهاتف والطباعة.
- يعيد health حالة قاعدة البيانات ويعرض النظام صفحات أخطاء مخصصة.
